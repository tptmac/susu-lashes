import React from 'react';

import { PageType } from '../../constants';
import Page from '../common/Page';

function CarePage() {
  return (
    <Page pageType={PageType.CARE}>
    </Page>
  );
}

export default CarePage;
